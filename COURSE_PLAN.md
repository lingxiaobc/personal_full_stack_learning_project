# 全栈软件工程师成长课程安排

版本：1.0 · 设计日期：2026-09-08 · 学习记录更新：2026-09-14 · 状态：课程执行中；L08 已通过，L09 已开始讲解并待实作验收。

目标：在 AI 的帮助下，你能把需求变成可运行的全栈应用，解释主要数据流，判断实现取舍，依据证据定位故障，完成修复、验证与恢复。成长以能处理的任务和问题为准，不以记住多少命令、学过多少框架为准。

课程由 **48 节共同主线课、9 个阶段关卡、4 条方向各 3 节进阶课**组成。默认先完成共同主线，再选一条方向。每节课是一项能力单元，必要时可拆成多次学习；本文件不代表已经安装、实现或完成其中的系统。

快速导航：[当前起点](#current) · [路线与时间](#roadmap) · [验收和补课](#assessment) · [逐课安排](#lessons) · [方向选修](#branches) · [学习记录模板](#records) · [下一次学习](#next) · [参考依据](#references)。

## 1. 当前起点与培养目标

<a id="current"></a>

目前项目可确认的组件：

| 组件 | 当前文件 | 作用与边界 |
| --- | --- | --- |
| 工作区与启动编排 | [package.json](package.json) | 同时或分别启动前后端；具体行为继续看各 workspace 的 scripts |
| 页面与浏览器逻辑 | [frontend/src/App.tsx](frontend/src/App.tsx) | 输入、state、前端校验、fetch、响应解析和结果显示 |
| 页面入口与样式 | [frontend/src/main.tsx](frontend/src/main.tsx)、[frontend/src/index.css](frontend/src/index.css) | 挂载 React、组织页面外观 |
| 后端与 API | [backend/src/server.ts](backend/src/server.ts) | 监听 3000、处理 CORS、解析 JSON、校验并返回响应 |
| 协作规则 | [AGENTS.md](AGENTS.md) | AI 修改文件；你阅读 diff、观察结果、解释因果 |

现有源码没有数据库、账户和任务队列。课表里提到的这些组件是未来新增内容。浏览器访问的前端端口以实际启动输出为准；当前后端允许的前端来源包括 5173 和 5174。

你已经接触过启动命令、npm workspace、端口、请求响应、Console、Network、Sources、前后端校验及 CORS 预检。它们列为“已接触、待验收”，允许通过短测跳过讲解，不直接标记为“掌握”。

特别注意：当前前端会阻止空姓名提交；直接向后端发空姓名请求才会得到后端 400。README 中“留空发送观察后端 400”的页面操作说明已落后于代码，实验应以实际请求为准。本轮不修改 README。

毕业能力按以下具体表现判断：

| 能力 | 你需要能做到什么 |
| --- | --- |
| 需求与设计 | 把模糊需求写成用户流程、数据模型、接口、边界条件和可检查的验收 |
| 全栈实现 | 指导 AI 按小步构建页面、API、数据库、身份与权限，并阅读关键 diff |
| 系统理解 | 解释一次操作在哪里运行、数据如何转换、在哪里保存、谁负责校验 |
| 诊断与修复 | 先收集证据，再缩小问题层级；提出最小修复并验证正常与失败路径 |
| 工程交付 | 用 Git、测试、构建和环境配置交付；能恢复版本和验证数据恢复 |
| 技术判断 | 能比较简单方案与复杂方案的收益、成本、限制，知道何时需进一步调查 |
| AI 协作 | 能指出 AI 的假设、错误或越界修改；不会把生成成功或回答自信当成验证 |

共同主线通过后，目标是能在 AI 帮助下交付和维护中小型 Web 应用。大规模生产经验、资深架构能力和就业结果需要后续实践，不能由课表完成度直接推出。

## 2. 成长路线、项目与时间安排

<a id="roadmap"></a>

主项目默认从“HTTP 数据流实验”成长为“个人学习记录 → 多人协作任务系统”。先沿用 React、TypeScript、Vite、Express；持久化阶段建议选 PostgreSQL，先学习 SQL 再判断数据访问工具。具体安装方式与库版本在开课时结合环境核对，不要求现在安装。

| 阶段 | 课程 | 角色能力 | 阶段产物 | 进入条件 |
| --- | --- | --- | --- | --- |
| 0 环境与阅读基础 | L01–L04 | 能运行、找文件、读最小逻辑 | 可复现启动说明与恢复演练 | 无；已接触内容可挑战验收 |
| 1 一条请求的完整生命线 | L05–L10 | 能追踪和定位请求失败 | 可解释、可排错的 HTTP 实验 | 阶段 0 通过 |
| 2 可维护的前端 | L11–L16 | 能构建交互并解释状态 | 可访问、可切页的学习记录界面 | 阶段 1 通过 |
| 3 API 与业务建模 | L17–L22 | 能实现有边界的业务 API | 内存版学习记录 CRUD | 阶段 2 通过 |
| 4 数据库与一致性 | L23–L28 | 能持久化并保护数据关系 | 数据库版记录与项目管理 | 阶段 3 通过 |
| 5 多用户与安全边界 | L29–L33 | 能识别身份与数据归属 | 有登录、权限、隔离的协作应用 | 阶段 4 通过 |
| 6 测试、部署与恢复 | L34–L38 | 能验证、交付、恢复 | 可重建部署与故障处理手册 | 阶段 5 通过 |
| 7 复杂业务与运行质量 | L39–L43 | 能处理并发、慢请求和异步任务 | 可测量、可诊断的模块化应用 | 阶段 6 通过 |
| 8 独立交付训练 | L44–L48 | 能主持一次完整交付 | 陌生业务毕业项目与证据包 | 阶段 7 通过 |

每课默认依赖同阶段前一课；阶段首课依赖上一阶段关卡。允许提前阅读兴趣内容，但不能据此跨过相关实作门槛。阶段末课本身就是综合关卡，不另造重复作业。

普通课先按 2–4 小时安排，阶段关卡按 6–10 小时安排，毕业阶段约 20–40 小时；含复习和返工的共同主线先预留约 160–280 小时。这是初始教学估算，前两周根据实际耗时调整，不承诺固定日期毕业。每周 6–8 小时时，先按约 5–11 个月规划；已掌握内容可以短测压缩，基础薄弱内容可以延长。

建议一周安排两次动手课和一次复测。每次开始前先回忆上课的数据流；一节课没通过，就把下一次学习用于补齐该项，不继续堆功能。进阶方向每条另预留约 12–24 小时。

## 3. 每节课怎样上、怎样决定过关

<a id="assessment"></a>

每课沿用一个顺序：**先预测 → 观察正常行为 → 读关键代码 → 做单点改动 → 诊断失败 → 修复回归 → 换场景复测**。

AI 在动手前说明目标、改动位置、预期影响和恢复方式。你先回答“我预计哪里会变化、准备看什么证据”，然后 AI 执行已授权的文件修改；你通过 `git diff` 检查。教学实验可以告知故障点，正式验收的变式不提前透露具体原因。

正常代码仍由 AI 编写。随着阶段推进，逐渐撤去的是判断提示：阶段 0–1 给出观察位置；阶段 2–4 由你先提出检查顺序；阶段 5–7 由你给出假设与反证；毕业项目由你写任务、验收、修改边界和修复判断。背诵语法与手写速度不作核心门槛，但必须能读懂实际变更的关键逻辑。

### 3.1 四维评分与必过项

| 维度 | 0 分 | 1 分 | 2 分 |
| --- | --- | --- | --- |
| 解释 | 不能指出输入输出和责任层 | 看答案后能复述 | 不看答案，结合当前代码解释输入、处理、输出和原因 |
| 证据 | 只说“好像好了” | 在指引下找到证据 | 主动选择日志/请求/数据/测试并说明支持或排除什么 |
| 迁移 | 只会复现原例 | 换例后需要关键提示 | 对未演示的同类变式先预测，再定位或提出正确最小改动 |
| 验证与恢复 | 未复测或破坏其他行为 | 需提醒才复测 | 明确要求 AI 修复，检查故障路径、正常回归和实验恢复 |

**进入下一课须四项各得 2 分，且该课“验收”中的所有条件均满足。** 不用总分抵消某项缺失。你可以查资料、用 AI 写代码和执行命令；验收时必须先给出自己的判断，再让 AI 帮助验证。如果 AI 先泄露根因或代答，本次只算练习，改用新的等价题复测。

AI 根据你的回答和实际证据给暂定评分，同时写明依据；证据不足标记“待复测”，你可以要求复核。阶段关卡需包含一次跨层故障，并在下一次学习开始时做一次不同输入或入口的短复测。新学课程不必机械等满几天，但不能只凭刚看过的答案通过。

不通过的处理：记录失分维度 → 回到本课最小例子或对应先修课 → AI 示范一次 → 换参数、字段、请求入口或业务重新测。连续两次不过则拆小课程，先补“变量/条件/异步”“HTTP 边界”或“工具观察”等具体短板，不用加大作业量处理理解障碍。

### 3.2 实验与故障处理通用规则

每课的“实验与诊断”是指定场景，“验收”是检查结果；下列恢复规则适用于所有课程，不要求在每课重复写一遍：

1. 开始前记录 Git 状态、基线行为、允许改动文件和恢复点。保留已有改动；恢复只撤销本次实验 diff。数据库实验先准备可重建的合成数据或备份。
2. 只改变一个主要变量。故障实验限定当前本地练习项目；安全、并发、负载实验使用隔离环境和少量合成数据。
3. 记录“现象—假设—检查—证据—结论”。区分页面、浏览器网络、服务进程、API、数据库及外部依赖，不能因为页面报红就说后端崩溃。
4. AI 完成最小修复后，检查原失败用例、一个正常用例和受影响回归。数据库变化额外核对记录与约束，配置变化实际重启验证。
5. 结束时明确保留的功能和撤销的故障。无法恢复时暂停相关课程，说明卡点和下一项证据，不能标为通过。

部署课默认在本机或隔离练习环境完成；真实发布、外部写入、付费和使用真实数据在当课另行确认。本课程方案本身不授权这些操作。

## 4. 共同主线：逐课安排

<a id="lessons"></a>

### 阶段 0：认识自己的开发环境

#### L01｜从运行命令找到实际执行入口

- **核心主线**：解释“我运行一条命令，究竟启动了哪个程序”。
- **重点知识**：目录与相对路径、进程、终端、Node/npm、package.json 与锁文件、workspace、开发/构建/运行的区别。
- **方法与产物**：从根 scripts 追到子包 scripts，再追到入口文件；画出前后端两个进程及端口，保存最短启动说明。
- **实验与诊断**：在根目录和子目录对照命令；识别 PowerShell 脚本限制、缺依赖、找不到 script 属于哪一层。使用 npm.cmd 作为 Windows 启动入口的观察对照，不在课程中默认改系统策略。
- **验收**：找到并解释三个 package.json；分别说明 dev、build、后端 start 的输入和产物；对一个启动失败选对检查位置并验证恢复。不能假定根目录有 start。

#### L02｜读懂最小程序，而不是逐行背诵

- **核心主线**：追踪一个值怎样经过函数和条件得到结果。
- **重点知识**：字符串、数字、布尔值、对象、数组、变量、函数参数/返回值、作用域、条件、循环、模块导入导出。
- **方法与产物**：用当前姓名处理逻辑列出 `"张三"`、空串、缺失字段、数字四种输入的中间值和返回路径。
- **实验与诊断**：AI 在本地小函数中制造字段拼写、字符串与数字混用问题；用打印或断点检查实际值及类型。
- **验收**：在执行前正确预测四种输入；解释 `undefined` 与空字符串的区别；对未演示的字段错误提出正确修复并观察结果。

#### L03｜异步、JSON 与 TypeScript 的边界

- **核心主线**：理解“发起操作”和“拿到结果”不是同一时刻。
- **重点知识**：Promise、async/await、try/catch、JSON 序列化/解析、类型标注与运行时校验、编译与运行。
- **方法与产物**：对当前 fetch 画时间顺序；列出对象、JSON 文本、Response 对象和解析后的对象分别是什么。
- **实验与诊断**：在隔离例子中去掉 await 或传入类型错误的 JSON 数据，观察 Promise、编译错误或运行时错误的位置。
- **验收**：解释类型声明为什么不能替后端校验网络输入；预测缺少 await 的现象，定位并要求最小修复；正常/非法输入均复测。

#### L04｜关卡：用 Git 保存一次可恢复的实验

- **核心主线**：让每次学习都有能解释的变更和恢复点。
- **重点知识**：工作区、暂存区、commit、diff、日志、分支、忽略文件；恢复代码不等于恢复数据库。
- **方法与产物**：指导 AI 只改一处展示文字；检查 status/diff，写出提交说明和具体恢复方案；提交动作按当次授权执行。
- **实验与诊断**：比较“源文件已改但看错页面端口”和“修改未保存到目标文件”，用路径、进程和 diff 排除猜测。
- **验收**：从正确目录启动并指出入口；说明改动影响和范围；恢复指定实验改动且保留原有变更。提交不存在时不编造 commit 记录。

阶段关卡产物：启动说明、最小数据转换解释、一次可恢复 diff。L01–L04 全过才进入阶段 1。

### 阶段 1：沿着一次请求理解系统

#### L05｜输入事件如何变成请求

- **核心主线**：追踪输入框 → 事件处理 → state → submit → fetch。
- **重点知识**：DOM Event、受控输入、React state、preventDefault、请求发起时机；Console 日志与真实网络请求的区别。
- **方法与产物**：在 App.tsx 指出输入与提交两段逻辑，给一次点击制作“动作—变量—日志—请求”对照表。
- **实验与诊断**：只输入不提交、正常提交、空白提交各一次；通过 Network 判断是否真正发送。
- **验收**：正确解释三种场景的请求数量与页面变化；对“有输入日志但没请求”的新例子定位到事件/校验阶段。

#### L06｜请求与响应的内容分别在哪里

- **核心主线**：将浏览器看到的信息与后端收到的信息一一对上。
- **重点知识**：URL、Method、Headers、Body、Content-Type、状态码、响应体；收到响应与业务成功的区别。
- **方法与产物**：记录一次请求的 Payload、后端 req.body、响应状态及解析后数据；解释 fetch 与 response.json 的职责。
- **实验与诊断**：正常数据和直接请求的空数据各一次，比较 200、400 与页面状态。
- **验收**：从 Network 定位字段和响应；解释 400 不代表没有响应、响应体也不是每种响应都必有；对非 JSON 响应不盲目解析为 JSON。

#### L07｜跨域预检和请求到底发给谁

- **核心主线**：分清来源、目标以及浏览器对跨域响应的访问限制。
- **重点知识**：scheme/host/port 组成 origin；OPTIONS 预检与实际 POST；CORS 与身份验证的区别。
- **方法与产物**：标记前端 origin、API URL、预检请求和实际请求；对照浏览器请求与终端请求的观察位置。
- **实验与诊断**：AI 在本地暂时制造来源不匹配；核对响应头和后端日志。预检可能被缓存，不以“每次必须出现两条”为判断标准。
- **验收**：能区分同名 hello 的方法与内容；说明终端请求不会自动出现在浏览器 Network；不能把 CORS 当后端授权校验；恢复后重测。

#### L08｜字段契约不一致：有数据为什么仍失败

- **核心主线**：前后端对同一份数据的字段名、类型和含义必须一致。
- **重点知识**：API Contract、字段读取、undefined、运行时校验；“已到达”与“已被正确理解”的区别。
- **方法与产物**：写出 `POST /api/hello` 的请求、成功/失败响应；对照 Payload 与后端读取表达式。
- **实验与诊断**：AI 只把后端读取 `request.body?.name` 暂改为 `request.body?.username`；前端仍发 name。你追踪数据在哪里失配，之后由 AI 恢复。
- **验收**：发送非空姓名也出现 400 时，能用请求、日志和代码解释因果；恢复后得到 200；再对一个不同字段/类型的变式定位，不只记住改 name。

#### L09｜把“请求失败”拆成不同原因

- **核心主线**：根据证据区分未连接、HTTP 错误、解析错误与前端显示错误。
- **重点知识**：连接失败、404、400、500、JSON 解析失败、异常堆栈、错误分类与错误提示。
- **方法与产物**：画出 fetch → 检查响应 → 解析 → 更新 state 的失败位置；给每一类错误指定首个检查工具。
- **实验与诊断**：逐一试错端口、错路径和响应非 JSON。当前 catch 会把解析等异常也提示成“无法连接后端”，以此训练检查提示是否符合根因。
- **验收**：不只根据提示文字归因；能用 Network/堆栈区分三类错误，指导 AI 改进分类并对正常响应、HTTP 失败和解析失败分别验证。

#### L10｜关卡：陌生故障的完整证据链

- **核心主线**：你主持一次定位和恢复，AI 执行你确认的最小修改。
- **重点知识**：假设与证据、排除法、单变量实验、前端体验校验与后端数据边界。
- **方法与产物**：提交一份“现象、两种假设、检查顺序、根因、修复、回归”的故障记录。
- **实验与诊断**：从错端口、字段失配、JSON 中间件位置错误中选一个未透露根因的故障；补测直接请求绕开页面校验后仍被后端拒绝。
- **验收**：先提出检查顺序再操作；证据能定位故障层；修复后正常姓名 200、直接空姓名 400、页面空姓名不发请求；能解释三者不同。

阶段关卡产物：完整请求契约和故障报告。通过后应能回答“请求走到哪里、在哪个边界出了问题”。

### 阶段 2：前端基础与可维护交互

#### L11｜HTML、CSS 与浏览器实际呈现

- **核心主线**：区分内容结构、样式规则和最终 DOM。
- **重点知识**：语义标签、表单、label、盒模型、布局、响应式、样式优先级、键盘操作与可访问性。
- **方法与产物**：用 Elements/Computed 检查当前输入与按钮；让 AI 调整窄屏布局，记录修改前后证据。
- **实验与诊断**：定位固定宽度造成溢出、样式被覆盖或 label 未关联的问题；每次只做一种。
- **验收**：窄屏可操作、键盘能完成提交、输入有明确标签；能从生效样式定位一个布局问题，而非只反复调数值。

#### L12｜组件如何协作

- **核心主线**：用 props 和回调解释父子组件的数据流。
- **重点知识**：组件职责、props、回调、状态归属、单向数据流、避免重复保存可推导状态。
- **方法与产物**：由 AI 抽出表单与结果展示组件；你标记每个组件的输入、输出和调用者，审查拆分 diff。
- **实验与诊断**：制造父子各存一份姓名导致不一致，定位唯一可信状态来源。
- **验收**：能说明状态应放在哪里；表单、结果与请求数据保持一致；拆分后既有校验和请求行为不变。

#### L13｜列表、标识和不可变更新

- **核心主线**：一条记录修改后，准确更新对应页面元素。
- **重点知识**：数组 map/filter/find、稳定 ID、key、对象引用、不可变更新、基础时间复杂度。
- **方法与产物**：构建本地内存学习记录列表，支持增删改；追踪记录 ID 从数据到组件的映射。
- **实验与诊断**：排序后编辑错行，比较数组下标与稳定 ID；观察直接修改对象造成的状态问题。
- **验收**：新增、排序、删除、编辑后目标正确；能解释一次查找大致遍历多少项，并指出错误来自标识还是更新方式。

#### L14｜异步页面状态与竞态

- **核心主线**：让页面在等待、失败和多次请求交错时保持正确。
- **重点知识**：loading/empty/error/success、请求取消、过期响应、Effect 依赖与清理、闭包中的值。
- **方法与产物**：添加搜索交互，用可控延迟观察请求顺序与响应顺序；画状态转换。
- **实验与诊断**：先搜 A 后搜 B，让 A 更晚返回，检查旧响应是否覆盖新结果；核对取消是否被误报业务错误。
- **验收**：最终显示最新搜索结果；等待与失败可辨识；解释为什么请求取消不等于后端操作一定撤销；相关旧行为正常。

#### L15｜页面导航与 URL 中的状态

- **核心主线**：打开、刷新、分享同一个 URL 都能进入正确界面。
- **重点知识**：路径参数、查询参数、前端路由、页面状态与可分享状态、浏览器前进后退。
- **方法与产物**：加入记录列表和详情页，把记录 ID/筛选条件放入合适的 URL 位置。
- **实验与诊断**：直接访问详情、刷新和浏览器后退；区分开发服务器页面回退问题与详情数据不存在。
- **验收**：三种入口指向同一条记录；不存在 ID 有明确结果；解释前端路由与后端 API 路由各由谁处理。

#### L16｜关卡：从需求得到可靠交互

- **核心主线**：用可观察用户行为验证一组前端改动。
- **重点知识**：用户流程、组件责任、视觉与状态反馈、回归清单、基本交互测试思路。
- **方法与产物**：你写“新增记录—筛选—打开详情”的验收，AI 完成 UI；数据暂用内存，明确刷新持久化不属于此阶段。
- **实验与诊断**：AI 注入列表标识或状态同步故障，你从事件、state、props、DOM 定位。
- **验收**：鼠标与键盘核心流程可用，空数据/错误/窄屏有明确行为；解释数据为何刷新后丢失，并完成一个交互变式与恢复。

阶段关卡产物：可解释的学习记录界面、组件关系和行为验收清单。

### 阶段 3：后端 API 与业务规则

#### L17｜先定义业务，再定义接口

- **核心主线**：把“做一个学习记录系统”拆成可验收的用户动作。
- **重点知识**：实体、属性、用例、业务规则、非目标、成功条件、边界输入。
- **方法与产物**：定义记录的标题、内容、状态和标识；写新增/读取/修改/删除的契约表及输入例子。
- **实验与诊断**：用“空标题”“不存在记录”“修改未提供的字段”挑战含糊需求，发现哪些决定不能交给代码猜测。
- **验收**：每个用例有输入、规则、输出、失败条件；能解释一项主动不做的功能；未知需求先明确再让 AI 实现。

#### L18｜路由、中间件与执行顺序

- **核心主线**：追踪请求在后端经过的处理顺序。
- **重点知识**：Method/Path 匹配、params/query/body、middleware、next、JSON 解析、统一错误处理。
- **方法与产物**：用顺序日志标注解析、路由和错误处理；先保持简单 handler，只有职责开始混杂才拆分。
- **实验与诊断**：AI 暂时把 JSON 解析移到路由之后或漏掉 next；观察 body 缺失与请求悬挂的不同表现。
- **验收**：能指出最后到达的处理层及下一层未执行原因；恢复正确顺序，正常与非法 JSON 请求均有合理响应。

#### L19｜实现内存版 CRUD

- **核心主线**：一次页面操作对应一次明确的数据读写。
- **重点知识**：REST 资源、GET/POST/PATCH/DELETE、稳定 ID、状态码选择、找不到记录的语义。
- **方法与产物**：由 AI 接通页面与内存记录 API；你列出每条接口对数据集合的影响。
- **实验与诊断**：查找错误 ID、连续删除同一记录，观察是否操作错对象；说明进程重启后内存数据消失。
- **验收**：增删改查均能从 Network 对照后端结果；不存在 ID 不意外创建或修改别的记录；状态码与契约一致。

#### L20｜校验、错误结构与业务失败

- **核心主线**：把不合格输入挡在边界，并向调用者提供可处理的错误。
- **重点知识**：字段类型/长度、缺失与空值、运行时 schema、错误码与展示文案、日志与对外错误信息。
- **方法与产物**：为记录输入建立正例/反例表，规范错误响应结构；比较静态类型与运行时校验职责。
- **实验与诊断**：直接请求跳过页面，发错误类型、超长标题、额外字段；核对是否被拒绝或按契约忽略，不能悄然写入敏感字段。
- **验收**：非法输入不改变数据；客户端能定位字段问题；内部堆栈不出现在对外响应，后端保留诊断证据。

#### L21｜列表查询与接口演进

- **核心主线**：让筛选、排序、分页的含义前后端一致。
- **重点知识**：query 参数、默认值、范围限制、稳定排序、分页元信息、兼容性、调用方影响。
- **方法与产物**：为列表加有限分页与筛选，写查询示例及响应结构；模拟增加可选字段。
- **实验与诊断**：发送负页码、错误排序字段和边界页；识别是查询契约不清还是页面错误理解响应。
- **验收**：相同条件得到稳定次序，越界与非法参数有约定行为；旧调用方在兼容变化后仍能工作；解释破坏性变更影响。

#### L22｜关卡：把一项需求交给 AI 并验收

- **核心主线**：你负责需求和正确性，AI 负责范围内实现。
- **重点知识**：垂直切片、任务边界、diff 审查、接口 smoke test、回归覆盖。
- **方法与产物**：你指定“将记录标为完成并按状态筛选”，写文件范围、接口、失败用例和验收；AI 完成最小改动。
- **实验与诊断**：从“接口成功但列表未更新”或“状态字段含义不一致”中选一个变式，比较请求、存储和 UI。
- **验收**：完整用例通过且非法状态被拒绝；能指出每处关键 diff 影响哪条链；完成一份别人可照做的接口验证记录。

阶段关卡产物：内存版全栈 CRUD、接口契约、正反用例。此时不把进程内数据当作持久化。

### 阶段 4：持久化、SQL 与数据一致性

#### L23｜数据到底存在哪里

- **核心主线**：区分页面状态、浏览器存储、后端内存和数据库。
- **重点知识**：持久化、数据库服务、连接信息、数据库/表/行/列、主键、数据类型。
- **方法与产物**：建立本地练习数据库，设计 records 表；画页面到数据库的数据路径，记录连接目标但不记录秘密值。
- **实验与诊断**：分别刷新页面、重启后端、重启数据库，核对数据是否仍在；模拟连接错库或数据库未启动。
- **验收**：能证明应用连接的具体练习库，数据持久保存；从连接日志和查询区分“没保存”与“看错数据库”。

#### L24｜用 SQL 精确读写数据

- **核心主线**：在真实数据表里看懂增删改查的影响范围。
- **重点知识**：SELECT/INSERT/UPDATE/DELETE、WHERE、ORDER BY、LIMIT、参数化查询、受影响行数。
- **方法与产物**：先写查询预览目标行，再让 AI 执行练习数据更新；把原内存接口替换为数据库读写。
- **实验与诊断**：在可回滚的合成数据事务中比较正确条件与漏条件的更新范围；练习参数绑定，避免拼接输入。
- **验收**：能预测并核对行数；只改目标记录；异常输入不会改变 SQL 结构；修复后 API 与数据库结果一致。

#### L25｜关系、约束与数据模型

- **核心主线**：把项目与记录的关系落实到数据库规则。
- **重点知识**：一对多、多对多概念、外键、唯一约束、非空约束、JOIN、删除策略。
- **方法与产物**：新增 projects 与 records 的关系，画简明 ER 图；写出删除项目时记录如何处理的规则。
- **实验与诊断**：尝试引用不存在的项目、重复唯一值；比较前端校验与数据库约束分别捕获什么。
- **验收**：能解释 JOIN 返回的每列来自哪里；孤立引用和重复值按规则处理；错误可映射为合理 API 响应。

#### L26｜Schema 迁移与数据访问层

- **核心主线**：改变表结构时保持代码、数据库和已有数据相互兼容。
- **重点知识**：migration、默认值、回填、版本顺序、驱动与 ORM 的职责、SQL 可见性。
- **方法与产物**：用版本化迁移增加一个字段；你先解释 SQL，再决定是否使用数据访问工具；保存从空库构建步骤。
- **实验与诊断**：应用先更新而数据库迁移未执行，观察缺列错误；在练习库比较空库与有旧数据的迁移结果。
- **验收**：两类数据库都能迁移，旧记录得到约定值；能定位代码/数据库版本失配；说明何时应前向修复而非盲目回滚结构。

#### L27｜事务与最基本的并发正确性

- **核心主线**：两步业务写入要保持整体一致，并处理同时修改。
- **重点知识**：事务、提交/回滚、原子性、隔离的作用、丢失更新、条件更新、数据库约束。
- **方法与产物**：实现“移动记录并写变更历史”，画事务范围；用两次并发更新比较预期与实际。
- **实验与诊断**：在第二步故意抛错，检查是否半成功；模拟两方更新同一版本，核对是否静默覆盖。
- **验收**：失败时两个写入都未生效；并发冲突按规则显式处理；能解释仅使用事务为什么不自动解决所有并发问题。

#### L28｜关卡：数据库版应用的恢复演练

- **核心主线**：证明保存、查询、迁移和恢复都可靠。
- **重点知识**：备份、恢复验证、数据一致性、最小数据检查、应用版本与库结构配合。
- **方法与产物**：在合成数据上建立备份，恢复到另一个明确命名的练习数据库；记录数据量与关键关联的前后对照。
- **实验与诊断**：从错连接目标、未迁移或关系约束错误中抽一个故障，先取证再恢复；不覆盖原库进行恢复验证。
- **验收**：重启后数据存在，CRUD/关系/事务用例通过；新库能重建且关键数据一致；说明“备份文件存在”为什么不等于可恢复。

阶段关卡产物：数据库版应用、ER 图、迁移与备份恢复证据。涉及数据变更时开始把数据回归列为必检项。

### 阶段 5：登录、权限与可信边界

#### L29｜身份、登录与会话

- **核心主线**：后端怎样知道两次请求属于同一个已登录用户。
- **重点知识**：authentication、密码哈希、登录/退出、session、cookie、过期、密钥与环境配置。
- **方法与产物**：使用维护中的成熟方案构建本地登录，追踪登录响应、cookie 与后续请求；不自行设计密码学算法。
- **实验与诊断**：退出或过期后再次访问；用浏览器存储、请求头和后端会话记录检查“登录了却仍被拒绝”。
- **验收**：错误密码不登录，原密码不明文保存、不进入日志；登录后可访问，退出/过期后被拒；能解释身份在哪一层确认。

#### L30｜授权与数据归属

- **核心主线**：登录成功不代表能访问任意记录。
- **重点知识**：authorization、资源所有者、角色、按请求检查权限、401/403 与隐藏资源存在性的 404 策略。
- **方法与产物**：使用两个测试账户，把 owner 约束落实到后端读取和修改；写出角色/资源权限矩阵。
- **实验与诊断**：账户 A 直接请求账户 B 的记录 ID；核对后端是否只依赖页面隐藏按钮或用户提交的 ownerId。
- **验收**：越权读写均失败且 B 数据未改变；普通用户不能自授角色；响应策略与契约一致，日志足以定位拒绝原因。

#### L31｜浏览器与后端之间的安全边界

- **核心主线**：区分输入呈现、跨站请求、跨域读取与数据库执行的风险来源。
- **重点知识**：XSS、CSRF、CORS、输出处理、cookie 属性、参数化查询；不同控制保护不同边界。
- **方法与产物**：给当前数据流标出不可信输入和保护位置；按实际会话架构检查 CSRF 防护，不套用万能配置。
- **实验与诊断**：在隔离页面输入无害的标记文本观察转义；模拟不符合来源/防护要求的状态变更请求，检查拒绝及数据未变。
- **验收**：能解释各项保护的作用和盲区；不把开启 CORS 当作安全认证；正当操作仍可用，异常请求有证据可查。

#### L32｜文件、秘密与依赖的边界

- **核心主线**：上传内容与配置都必须有明确的存储、访问和披露范围。
- **重点知识**：大小/类型限制、服务端验证、文件名与存储路径、访问权限、环境变量、前端可见配置、依赖审查。
- **方法与产物**：给记录增加受限附件，使用服务器生成的存储标识；列出哪些配置可发到浏览器、哪些只留后端。
- **实验与诊断**：使用小型假文件测试类型不符与越权下载；用虚构密钥检查是否进入前端产物、Git diff 或日志。
- **验收**：非法上传不留无主资源，私人附件不能跨账户获取；知道文件扩展名不能单独证明类型；检查结果中无真实秘密。

#### L33｜关卡：两名用户的权限回归

- **核心主线**：用用户、动作和资源三者的组合证明系统边界。
- **重点知识**：权限矩阵、负例、会话失效、审计证据、控制与业务体验的平衡。
- **方法与产物**：整理匿名/A/B/管理员对查询、修改、附件的测试矩阵，标出允许与拒绝原因。
- **实验与诊断**：AI 在隔离环境注入一个归属检查遗漏；你从请求身份、查询条件和数据结果定位并指导恢复。
- **验收**：矩阵正反例全部符合契约；无越权读写、明文密码、前端秘密或错误恢复遗留；不以“没显示按钮”作为权限证据。

阶段关卡产物：多用户应用、权限矩阵、安全边界说明。只说明本课程范围的验证结果，不宣称已获得完整安全认证。

### 阶段 6：测试、部署与运维基本功

#### L34｜把人工验收变成有效测试

- **核心主线**：测试要能抓住真实错误，并保护关键行为。
- **重点知识**：单元/集成/端到端测试、确定性输入、测试隔离、断言、mock 边界、回归价值。
- **方法与产物**：为纯业务规则写单元测试，为数据库 API 写集成测试，为一条关键用户流程写端到端测试。
- **实验与诊断**：AI 故意改变一条规则，观察测试是否失败；检查只断言 200 却漏掉数据错误的弱测试。
- **验收**：代表性错误能被对应测试捕获，恢复后通过；测试可重复执行、不依赖运行顺序；说清哪些真实行为被 mock 掉了。

#### L35｜构建产物与环境差异

- **核心主线**：解释为什么开发可用、构建后却失败。
- **重点知识**：前端静态产物、后端编译结果、构建时/运行时变量、source map、锁文件与可复现安装。
- **方法与产物**：按实际 scripts 构建，在隔离目录验证产物和所需配置；列出 dev 服务提供了哪些便利。
- **实验与诊断**：模拟 API 地址构建时写错、配置缺失或大小写路径差异；对照源码、产物与启动日志。
- **验收**：能指出浏览器实际加载的产物、后端实际执行的入口；新环境按说明可构建运行；秘密不进入前端产物。

#### L36｜从进程到可访问服务

- **核心主线**：追踪浏览器 → 域名/网络入口 → 代理 → 应用进程 → 数据库。
- **重点知识**：Linux 基本目录/权限/进程/日志、监听地址、DNS、TLS、反向代理、静态资源、进程管理；容器镜像/容器/卷的基本边界。
- **方法与产物**：本地或隔离环境完成生产方式运行及代理配置；画拓扑，记录端口、进程与数据卷。可按环境拆为两次课。
- **实验与诊断**：分别模拟上游进程停止、代理目标写错或容器内误用 localhost；结合代理与应用日志定位 502/连接失败。
- **验收**：能沿拓扑检查每一跳；重启应用后数据不丢；能区分本机与容器的 localhost；未获授权不执行真实发布。

#### L37｜自动检查、发布与回退

- **核心主线**：让一个版本经过可检查步骤后成为可运行版本。
- **重点知识**：CI、安装/类型检查/测试/构建、制品、版本、环境配置、健康检查、兼容迁移、回退范围。
- **方法与产物**：编写并在本地验证流水线步骤与发布清单；具备远程权限后才接入实际平台。
- **实验与诊断**：制造测试失败或缺配置，确保流程停在正确位置；在练习环境回退一个兼容的应用版本。
- **验收**：坏版本不能通过检查；好版本可重建并完成健康检查；能说明代码回退对数据库是否兼容，不盲目退数据库结构。

#### L38｜关卡：一次完整的故障处理

- **核心主线**：在交付环境中复现、定位、止损、修复和复盘。
- **重点知识**：结构化日志、request ID、健康检查、故障影响范围、时间线、回归、恢复时间和数据损失范围。
- **方法与产物**：串联请求和后端日志，完成运行手册与一次事故记录；写清症状、根因、修复和预防措施。
- **实验与诊断**：从数据库不可用、错误环境配置或代理错误中选择一个未透露原因的故障；用合成数据练习。
- **验收**：你能按证据确定故障层并指导最小修复；核心流程、数据及权限回归通过；说明实际恢复时间与未验证项。

阶段关卡产物：可复现构建、部署拓扑、检查流程、恢复手册与事故报告。公开上线可作为后续获授权的延伸。

### 阶段 7：复杂业务、性能与架构判断

#### L39｜先测量，再优化

- **核心主线**：找出用户等待时间主要花在哪里。
- **重点知识**：浏览器请求瀑布、服务耗时、数据库查询、p50/p95、数据量、算法复杂度、索引与执行计划。
- **方法与产物**：用固定合成数据和有限请求量建立延迟基线，比较浏览器、应用和 SQL 时间；选择一个瓶颈改进。
- **实验与诊断**：观察缺索引或重复查询造成的变慢；保持输入与环境可比，避免把一次快响应当作优化证据。
- **验收**：给出前后数据和正确性回归；解释索引或缓存为什么可能没用；明确结果仅适用于本次负载范围。

#### L40｜重复请求、幂等与业务状态

- **核心主线**：一次意图重复到达时，系统不应无意产生重复业务结果。
- **重点知识**：重试、幂等键、唯一约束、状态机、条件更新、冲突响应、并发竞争。
- **方法与产物**：为“创建任务并提交”定义允许的状态转换和重复请求规则；对关键写入建立去重与冲突处理。
- **实验与诊断**：两次近乎同时提交、超时后重发、跳过中间状态；观察数据库是否重复或出现非法状态。
- **验收**：重复请求只产生约定的一次效果；非法状态转换被拒；能说明禁用前端按钮为什么不足以解决后端重复执行。

#### L41｜长任务、后台处理与重试

- **核心主线**：请求先得到受理结果，任务完成后再获得最终结果。
- **重点知识**：任务记录、worker、队列概念、状态查询、重试与退避、幂等消费、失败任务处理。
- **方法与产物**：实现小规模学习记录导出任务；先画 API、任务存储和执行进程的消息/状态路径，按需求选择最小实现。
- **实验与诊断**：任务执行中终止练习 worker，再恢复；观察任务卡住、重试和重复副作用。
- **验收**：用户能区分已受理、处理中、成功、失败；恢复后不重复产生业务结果；能用任务 ID 找到最后执行位置。

#### L42｜缓存与一致性的成本

- **核心主线**：读得更快时，解释数据可能为何变旧。
- **重点知识**：缓存键、TTL、失效策略、命中率、权威数据源、用户隔离、应用缓存与 HTTP 缓存的区别。
- **方法与产物**：仅为已测出瓶颈的查询加最小缓存，写出新鲜度约定；若无瓶颈，完成对照实验后可以移除缓存。
- **实验与诊断**：修改数据后查旧结果、切换账户查缓存，追踪键与失效逻辑。
- **验收**：新鲜度符合约定，无跨用户数据混用；有相同条件的性能证据；能论证保留或删除缓存的选择。

#### L43｜关卡：选择合适的系统边界

- **核心主线**：根据变化原因与故障范围决定模块边界。
- **重点知识**：模块化单体、职责分离、依赖方向、同步/异步接口、故障隔离、微服务成本、架构决策记录。
- **方法与产物**：给当前系统画模块和依赖；选择一次有实际收益的小拆分，写一页“问题、候选、取舍、验证”。
- **实验与诊断**：模拟一个模块变慢，观察影响范围；比较直接调用和任务处理的失败传播，不强行引入独立服务。
- **验收**：性能、重复提交和后台任务的关键回归通过；边界与业务责任对应；能解释为何当前规模保留单体或需要进一步拆分。

阶段关卡产物：测量报告、业务状态图、异步任务链路、架构决策。复杂组件必须能说明解决了什么已观察到的问题。

### 阶段 8：独立交付一个陌生业务

#### L44｜毕业立项：从一句话到可执行范围

- **核心主线**：你主持需求澄清和范围控制。
- **重点知识**：目标用户、主用例、非目标、约束、风险、可交付切片、验收条件。
- **方法与产物**：选择小型预约、借用、工单或内容协作中的一个新业务；交付一页需求、三个核心流程和非目标。
- **实验与诊断**：AI 提出模糊需求和范围扩张，你指出冲突、做取舍并修改验收。
- **验收**：核心流程有正例/反例和明确边界；无依赖未知权限的隐含承诺；能说明为什么先做选定切片。

#### L45｜设计与第一条贯通的数据链

- **核心主线**：将需求映射为 UI、接口、数据库与权限。
- **重点知识**：模型、关系、API 契约、归属规则、迁移、纵向切片。
- **方法与产物**：你先画数据流与模型，再指挥 AI 完成一个 UI → API → 数据库 → UI 的主用例。
- **实验与诊断**：检查字段、类型、时间或权限语义在各层是否一致；对一个新变式先预测。
- **验收**：首个用例的正常、非法输入、未授权和重启持久化均通过；关键设计能用实际请求和查询证明。

#### L46｜迭代、代码审查与团队式交接

- **核心主线**：持续增加功能而不丢失已有正确性。
- **重点知识**：小提交、分支、合并冲突、评审、测试回归、变更说明、维护文档。
- **方法与产物**：完成另外两个核心流程；由 AI 在练习分支制造一个文本/逻辑冲突，你说明双方意图再确定合并结果。
- **实验与诊断**：AI 提供一段含过度修改或缺失校验的 diff，你指出问题并提出收窄方案。
- **验收**：能拒绝无关改动，合并后双方必要行为保留；构建与关键测试通过，变更说明能让后来者复现。

#### L47｜上线前演练与未知故障

- **核心主线**：在干净练习环境交付，并处理未提示根因的问题。
- **重点知识**：重建、配置、依赖、迁移、权限回归、可观察性、备份与回退。
- **方法与产物**：按运行手册重建应用，准备演示数据和回退步骤；执行完整验收清单。
- **实验与诊断**：AI 从已学范围挑两个不同层的故障，逐个注入并在每次恢复后再进行下一个；不让多故障同时污染归因。
- **验收**：你先提出假设和检查，再指导 AI 修复；两次均给出证据链和回归；干净环境可复现，外部发布仍需另行授权。

#### L48｜毕业验收与能力复测

- **核心主线**：证明能力可以迁移到新需求和新故障。
- **重点知识**：需求取舍、跨层解释、诊断、变更影响、交付责任和已知限制。
- **方法与产物**：演示项目，随机抽一条操作解释全链路；处理一项未提前实现的小需求，交付毕业证据包。
- **实验与诊断**：给定与练习不同的输入/角色/入口，先预测结果，再通过实际证据验证；AI 保留编码帮助但不先代答。
- **验收**：满足下文毕业清单和四维评分；下一次学习用不同场景复测核心判断；若某项不过，回补对应课而非宣布全部毕业。

毕业证据包必须包含：需求与非目标、数据流/模型/权限说明、主要接口契约、可审阅 diff/版本记录、正常与失败测试结果、重建/恢复说明、两份跨层故障报告和已知限制。

毕业通过检查：

1. 三个核心业务流程可重复演示，非法输入、无权限、空数据和失败反馈符合契约。
2. 随机选一条操作，能指出真实代码、请求、数据表和日志，解释各组件输入输出。
3. 一项陌生小需求由你写验收、限定范围、审查 AI 修改，并完成回归。
4. 两个不同层的未知故障有“假设—证据—根因—修复—回归”记录，未用盲改碰运气。
5. 在干净练习环境按文档重建成功，持久化和恢复验证通过。
6. 能说清三个当前限制，以及扩展用户量或业务复杂度时应先测量或调查什么。

## 5. 共同主线之后：四条进阶方向

<a id="branches"></a>

共同主线毕业是以下实作课的共同先修，每个方向内部按 01 → 02 → 03 顺序。括号列出特别需要复习的课程。选修不能替代共同主线，也不默认扩展当前项目；进入分支时先确定具体练习范围。

| 方向 | 适合的目标 | 项目复杂性如何增加 | 特别需要的先修 |
| --- | --- | --- | --- |
| A 产品与前端深化（默认） | 快速交付体验完整的业务产品 | 公共页面 → 复杂交互 → 产品反馈闭环 | L11–L16、L35–L39、L44–L48 |
| B 后端与业务系统深化 | 处理复杂规则、协作和数据一致性 | 组织隔离 → 外部事件 → 多副本一致性 | L25–L33、L39–L43 |
| C 交付与运行可靠性深化 | 让应用更稳定、更易运维 | 运行目标 → 安全变更 → 故障恢复 | L28、L34–L43 |
| D AI 功能应用开发（可选） | 将模型能力接入已有业务 | 模型调用 → 可追溯检索 → 质量/成本评测 | L20、L29–L33、L38–L42 |

“使用 AI 帮助开发”贯穿所有课程；D 方向专指“产品内部提供 AI 功能”，不是成为全栈工程师的必修条件。默认推荐 A 是课程设计取舍：先强化完整产品交付；如果实际目标偏业务或运维，可以改选 B/C。无需现在选择。

### A：产品与前端深化

#### A01｜公共页面的渲染方式与性能

- **核心主线**：按用户入口与内容变化频率选择渲染方式。
- **重点知识**：CSR、SSR、预渲染、首次内容、hydration、搜索可发现性、数据加载边界。
- **方法与产物**：为毕业项目选一个公共页面，比较现有客户端渲染和一种服务端/预渲染方案；核对所选工具官方文档。
- **实验与诊断**：用 View Source、Network 和客户端日志定位服务端/客户端内容不一致及额外请求。
- **验收**：说明选择理由和新增运维成本；页面数据正确、可访问，有可比的首次呈现证据；修复一个渲染变式问题。

#### A02｜复杂表单与交互的一致性

- **核心主线**：多步编辑、草稿与提交保持同一套业务含义。
- **重点知识**：表单状态、字段依赖、草稿、服务端错误映射、可访问反馈、组件复用边界。
- **方法与产物**：构建有条件字段的多步表单，写出切换、返回、保存失败时的状态规则。
- **实验与诊断**：切换选项后残留旧字段、草稿覆盖新数据、服务器拒绝提交；沿状态和请求定位。
- **验收**：键盘可完成流程，隐藏字段按契约处理；草稿与提交数据一致；非法输入及保存失败不丢失用户可恢复内容。

#### A03｜方向关卡：用用户任务评价产品

- **核心主线**：通过真实操作证据改进体验和可靠性。
- **重点知识**：任务完成率、可用性观察、性能预算、错误反馈、隐私最小化、改进优先级。
- **方法与产物**：写三个用户任务，使用自测或自愿参与者观察记录，挑一个明确问题改进；参与者反馈未获取时明确标为自测。
- **实验与诊断**：把“找不到入口”和“接口慢”区分开，避免只改视觉而未解决任务阻碍。
- **验收**：改动对应观察证据，前后对比方法一致；核心流程与可访问性回归通过；能说明测量范围和未证实收益。

### B：后端与业务系统深化

#### B01｜组织与多租户隔离

- **核心主线**：同一套应用服务多个组织而不混用数据。
- **重点知识**：租户上下文、成员关系、资源归属、查询约束、缓存/任务携带租户、隔离策略取舍。
- **方法与产物**：给毕业项目增加两个合成组织，设计身份到组织再到资源的授权链。
- **实验与诊断**：改变组织 ID、跨组织查记录和复用缓存；检查每层是否绑定可信租户上下文。
- **验收**：API、后台任务和缓存均无跨组织读写；能解释每个隔离条件从何而来，修复遗漏并完成双组织回归。

#### B02｜外部事件与可靠对接

- **核心主线**：外部通知可能重复、延迟或乱序，业务仍能正确推进。
- **重点知识**：webhook、签名校验、重放、幂等、事件记录、状态转换、补偿/对账。
- **方法与产物**：用本地模拟服务对接“任务审批结果”，记录事件 ID 和处理结果；不连接真实支付或发真实消息。
- **实验与诊断**：发送重复、过期、签名错误或顺序相反的合成事件，观察状态变化。
- **验收**：不可信事件被拒，重复事件不重复生效，乱序按规则处理；能追溯事件与业务记录，说明补偿路径。

#### B03｜方向关卡：多实例下的业务正确性

- **核心主线**：不能依赖某个进程的内存来保证全局业务规则。
- **重点知识**：多副本、无状态服务、共享会话/存储、并发控制、事务隔离、故障切换、拆服务成本。
- **方法与产物**：在本地启动两个应用实例，对同一资源发送少量并发请求；画出状态实际存放的位置。
- **实验与诊断**：重启其中一个实例、把同一请求发到不同实例，定位内存锁/本地会话的失效。
- **验收**：核心不变量在并发和重启后保持；已有权限隔离回归通过；能用证据判断保持模块化单体还是进一步拆分。

### C：交付与运行可靠性深化

#### C01｜从日志到运行目标

- **核心主线**：定义何时算“服务正在正常工作”。
- **重点知识**：指标、日志、追踪、SLI/SLO、延迟/错误率、可用性窗口、可行动告警。
- **方法与产物**：为一个核心流程定义测量方式、观察窗口和目标，关联请求日志与阶段耗时。
- **实验与诊断**：在本地加入短时延迟和错误，检查测量是否定位瓶颈，区分进程存活与业务可用。
- **验收**：能用一段数据计算指标并解释局限；告警对应明确处理动作；修复后指标和业务用例都恢复。

#### C02｜容量、变更与发布策略

- **核心主线**：根据测量和兼容性控制一个新版本的影响范围。
- **重点知识**：负载基线、资源瓶颈、连接池、滚动/蓝绿概念、健康探针、配置与迁移兼容。
- **方法与产物**：做有限本地负载对照，选择一个发布策略并在练习环境演练旧/新版本切换。
- **实验与诊断**：新版本健康检查失败或数据库连接耗尽，比较资源指标、请求耗时和日志。
- **验收**：坏版本不承接正常流量，切换及回退有证据；数据和会话行为符合约定；不把本机结果推断成真实生产容量。

#### C03｜方向关卡：恢复能力而非恢复口号

- **核心主线**：按预案恢复服务和数据，并量化实际损失范围。
- **重点知识**：恢复时间目标/恢复点目标、备份恢复、演练、运行手册、复盘与后续行动。
- **方法与产物**：在隔离环境模拟服务与练习数据不可用，恢复到新环境，记录时间线与数据核对。
- **实验与诊断**：发现备份不完整、配置缺失或恢复顺序不对，按证据修正手册并重演受影响环节。
- **验收**：关键数据与业务恢复通过，实际恢复时间和缺失数据范围可核对；解释目标与实测差距，提出一个具体改进。

### D：AI 功能应用开发

#### D01｜把模型当作有失败模式的外部服务

- **核心主线**：浏览器通过自己后端调用模型，处理慢、错和不可用。
- **重点知识**：服务端凭据、超时、限额、流式响应、取消、结构化输出校验、成本上限。
- **方法与产物**：先用本地模型接口模拟器实现摘要功能；经授权后才接真实供应商，并重新核对官方 SDK/协议。
- **实验与诊断**：模拟超时、限流、输出结构错误和流中断，用请求 ID 对齐前端、后端与供应商日志。
- **验收**：秘密不在浏览器，坏输出不直接写业务数据；有清晰失败反馈、有限重试与成本边界；正常流程回归通过。

#### D02｜检索、引用与权限

- **核心主线**：回答只使用当前用户有权访问且能追溯的资料。
- **重点知识**：切分、索引、检索、引用、权限过滤、资料更新、提示注入、不可信内容边界。
- **方法与产物**：为一小组合成资料实现检索问答，记录检索命中、来源与最终引用，不一开始引入大规模知识库。
- **实验与诊断**：测试无答案、过期资料、跨账户资料与含干扰指令的文档；区分检索失败与模型忽略证据。
- **验收**：未授权资料不进入模型上下文，引用能找到原文；证据不足能说明不知道；干扰文档不获得执行权限或暴露秘密。

#### D03｜方向关卡：质量、成本与人工确认

- **核心主线**：用固定评测集判断 AI 功能是否值得交付。
- **重点知识**：离线评测、正确性/依据/拒答、回归、延迟、调用成本、人工审核、版本比较。
- **方法与产物**：建立包含正常、模糊、无答案与越权问题的小评测集，对比两种提示或检索方案。
- **实验与诊断**：某项优化降低成本却增加无依据回答，检查失败样例而非只看平均分。
- **验收**：报告固定输入下的质量/成本/延迟及局限；敏感写入保留明确人工确认；退回旧方案后业务与权限仍正确。

## 6. 学习进度与可复用记录

<a id="records"></a>

本节提供模板，并在课程实际开始后由 AI 按证据维护 `learning/` 下的当前课记录。当前已保存 L08 的实际学习记录；后续不会预建 60 份空记录。每课只保存足够复现的证据，不收集密码、cookie、token 或私人数据。

### 6.1 当前状态

| 项目 | 状态 | 下一动作 |
| --- | --- | --- |
| L01 的部分概念、L05–L07 的部分观察、L10 的校验边界 | 已完成入门短测范围，尚未逐课验收 | 保留已有证据；只补缺少的语言、异步、预检或恢复变式 |
| L02–L04 的基础阅读与恢复能力 | 尚未系统验收 | 在后续短测或相关故障中判断是否需要完整补课 |
| L08 字段契约不一致 | 已通过（2026-09-14）；字段名失配与无提示类型变式均已恢复并回归 | 进入 L09；保留 [L08 实际学习记录](learning/L08-api-contract.md) 作为后续错误分类的对照证据 |
| L09 请求失败分类 | 已开始概念讲解，未完成实作验收 | 依次观察请求构造失败、连接失败、HTTP 失败与响应解析失败；不把它们都称为“后端崩了” |
| 其余课程 | 未开始 | 依先修与验收推进 |

“未记录通过”表示缺少本课规定的验收证据，不否定你已经学会的部分；可以直接做挑战题证明。

### 6.2 每课记录模板

```text
课程 ID / 标题：
日期 / 实际用时：
状态：未开始 / 学习中 / 待复测 / 已通过
先修检查：
本课核心主线（用自己的话）：
我的事前预测：
涉及文件 / 组件 / 数据流：
AI 的修改范围与关键 diff：
正常行为证据（操作、输入、期望、实际）：
故障证据（复现、日志/请求/数据/堆栈）：
我的假设与排除过程：
最小修复及为什么有效：
正常 / 失败 / 受影响回归结果：
恢复点与最终状态：
变式题与我在提示前的回答：
评分：解释 __/2，证据 __/2，迁移 __/2，验证与恢复 __/2
逐项验收：通过项 / 未通过项 / 依据
AI 提示程度：无关键提示 / 指出工具 / 指出根因 / 直接代答
暂定结论与争议项：
补课或复测安排：
下一课及进入依据：
```

### 6.3 诊断记录模板

```text
现象：用户做了什么，预期什么，实际什么？
范围：哪些功能/用户/环境受影响，哪些仍正常？
基线：最后一次正常的版本、配置与数据条件？
假设 1 / 假设 2：分别需要什么证据支持或推翻？
首个检查：为什么先查这一处？
已验证事实：实际输出；不要把推测混进来。
结论：数据在哪个边界偏离预期，具体原因是什么？
修复：允许修改哪里，为什么是最小充分改动？
验证：原失败场景 + 正常场景 + 相关回归。
恢复：留下什么，撤销什么，数据是否需要额外恢复？
后续：怎样更早发现，是否值得增加日志/测试/约束？
```

### 6.4 向 AI 布置下一课的模板

```text
请按 COURSE_PLAN.md 开始 Lxx。
先读取当前代码、上课记录和 Git 状态，核对先修是否通过。
代码由你修改，我看 diff；先讲清主线、组件与输入输出。
训练时给我观察方法；验收前让我先预测和提出检查顺序，
不要提前透露变式根因，也不要替我完成解释。
修改后带我对照页面、Network、日志和需要的数据检查。
故障必须恢复并做回归；记录评分与证据，未过关先补课。
```

## 7. 下一次就怎样开始

<a id="next"></a>

当前继续点是 L09。先阅读 [L08 实际学习记录](learning/L08-api-contract.md) 和当前 Git 状态；L08 的临时前端故障已恢复，构建通过，正常 `POST /api/hello` 已回归 HTTP 200。

1. 先预测再执行单变量场景：浏览器在发送前拒绝请求、错误端口导致没有可用 Response、错误路径返回 404、非 JSON 响应导致 `response.json()` 失败。
2. 每个场景都记录“请求是否发出、是否得到 Response、状态码、响应体格式、是否进入 catch、页面是否更新”，并同时检查 Network、Console、后端终端与页面。
3. 明确区分：HTTP 4xx/5xx 是已收到 Response 的业务/路由结果；`catch` 表示 Promise 被拒绝或后续处理（如 JSON 解析）抛出异常，二者不能互相替代。
4. 观察完成后再最小化修改前端错误分类，避免把所有 `catch` 一律提示为“无法连接后端”。修改前不提前改变业务代码。

L09 只有在能独立区分上述错误层级、并在正常响应、HTTP 失败和解析失败三种路径分别验证后，才记录为已通过。

## 8. 参考依据与维护方式

<a id="references"></a>

课表顺序、课时估算、项目路线与评分门槛是为本项目设计的教学安排，不是某个机构提供的认证标准。设计时核对了以下官方资料，具体课程开展前还要以项目安装版本对应的官方文档核对 API 和配置。

- [MDN Curriculum：Core modules](https://developer.mozilla.org/en-US/curriculum/core/)：用于核对 HTML、CSS、JavaScript、可访问性和版本管理等基础覆盖；本课程将这些基础嵌入已有项目中学习。
- [PostgreSQL：Transactions](https://www.postgresql.org/docs/current/tutorial-transactions.html)：作为事务原子性、提交/回滚与并发可见性入门依据，对应 L27；练习不把事务泛化为所有并发问题的自动解法。
- [OWASP ASVS](https://owasp.org/www-project-application-security-verification-standard/)：作为应用安全控制与可验证要求的参考，对应 L29–L33 和选修隔离练习；本课表不宣称覆盖完整标准或通过认证。

每完成一个阶段，检查实际耗时、重复失败点、已掌握证据和下一阶段的必要性；允许缩短讲解、拆课或替换等价业务，但保留核心主线和验收。工具升级只在能解决具体问题时引入，更新前后保留相关行为验证。

课程设计的诊断、取舍、依赖与学习假设另存于 [设计方案](docs/full-stack-course-design-plan.md)，按本次指定的 plan-tasks 技能进行结构检查。日常学习以本课表与实际学习记录为入口；本轮不创建课程执行 tracker，不自动开展全部实验。
